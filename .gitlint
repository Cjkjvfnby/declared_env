[general]
# Ignore certain rules (comma-separated list), you can reference them by
# their id or by their full name
ignore=body-is-missing

[title-match-regex]
regex=^(?P<capital_or_symbols>[^a-z0-9]).*
